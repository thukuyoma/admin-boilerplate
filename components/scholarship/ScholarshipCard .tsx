import React from 'react'
import styled from 'styled-components'

const Styles = styled.div``
export default function ScholarshipCard({ scholarship }) {
  return <Styles>{JSON.stringify(scholarship)}</Styles>
}
